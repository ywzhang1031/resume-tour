# 首版验证记录

日期：2026-09-07。

## 已通过

- TypeScript 类型检查。
- 8 项行为测试：稳定 ID 改名、排序、隐藏/草稿/归档过滤、悬空引用、重复 ID、新项目默认展示、链接与笔记路径、讲述位置和组合搜索。
- 生产构建；首页、在线简历与 PDF 请求成功。
- PDF 两页，逐页渲染检查，无截断和乱码；生产下载文件与本地产物 SHA-256 一致。
- 1366 px 桌面和 390 px 手机布局检查，未发现横向溢出。
- 首页四组技术栈及关联入口；按 NV12 搜索返回两个工作项目。
- 降落保护章节方向键切换；打开详情、关闭后返回原步骤和触发按钮。
- 关闭 3D 后仍有完整数据流文字；生产 LLM Infra 章节可切换步骤并渲染 WebGL。
- 生产页浏览器控制台检查无 error / warn。
- npm audit：当前锁定依赖报告 0 项漏洞。

## 范围

交互与视口验证在本地生产构建的 Chromium 预览中进行。线上页面与 PDF 通过带所有者身份的 HTTP 请求验证；线上浏览器最终复核因电脑锁屏未完成。系统减少动态效果的处理已接入，尚未在各类辅助技术与所有移动设备上逐一测试。网站不是完整的离线应用。人物形象是图片，技术流程才使用 WebGL。

## 本地生产运行

生产构建会重新写入输出目录。构建后重启 `npm start`，不要以构建过程中仍运行的旧预览判断静态文件内容。

持久预览已发布：https://yuewen-resume-tour.anzhang.chatgpt.site 。当前只允许所有者访问，外部访客为 0。此地址不依赖本机服务。

GitHub CI 的内容、类型、行为和生产构建检查已通过，运行记录：https://github.com/ywzhang1031/resume-tour/actions/runs/34086955849 。
